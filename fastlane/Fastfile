org, repo = (ENV["GITHUB_REPOSITORY"] || "").split("/")
match_org, match_repo = (ENV["MATCH_REPOSITORY"] || "").split("/")

default_platform(:ios)

platform :ios do
  # centralized match call with our params
  private_lane :run_match do |options|
    match(
      type: "appstore",
      readonly: options[:readonly],
      git_url: "git@github.com:#{ENV["MATCH_REPOSITORY"]}.git",
      git_branch: ENV["MATCH_GIT_BRANCH"] || "main",
      app_identifier: [
        ENV["IOS_BUNDLE_ID"],
        "#{ENV["IOS_BUNDLE_ID"]}.notifications"
      ]
    )
  end

  lane :ensure_certs do
    setup_ci
    app_store_connect_api_key(
      key_id: ENV["APPSTORE_KEY_ID"],
      issuer_id: ENV["APPSTORE_ISSUER_ID"],
      key_content: ENV["APPSTORE_P8"],
      is_key_content_base64: false
    )

    # сначала максимально "read-only" (как в CI)
    begin
      UI.message("🔐 match: readonly=true")
      run_match(readonly: true)
    rescue => e
      UI.important("readonly failed, fallback to write mode: #{e}")
      run_match(readonly: false)
    end
  end

  # === основной CI-лейн (как в CI_Core) ===
  lane :init_ci do
    # 1) авто-создание deploy key и настроек через плагин
    github_action(
      api_token: ENV["GH_PAT"],
      org: org,
      repo: repo,
      match_org: match_org,
      match_repo: match_repo
    )

    # 2) сертификаты/профили
    ensure_certs

    # 3) Pods внутри Fastlane (как у тебя)
    begin
      install_pods
    rescue => e
      UI.important("Pods install skipped/failed: #{e}")
    end

    # 4) Сборка через workspace, если есть; иначе — через project
    workspace_path = Dir["*.xcworkspace", "ios/*.xcworkspace"].first
    if workspace_path
      UI.message("📦 Using workspace: #{workspace_path}")
      build_app(
        workspace: workspace_path,
        scheme: ENV["IOS_SCHEME"] || ENV["PROJECT_NAME"],
        export_method: "app-store",
        clean: true,
        xcargs: "-allowProvisioningUpdates"
      )
    else
      UI.message("📁 No .xcworkspace found, building Xcode project")
      build_app(
        project: ENV["XCODE_PROJECT"] || "#{ENV["PROJECT_NAME"]}.xcodeproj",
        scheme: ENV["IOS_SCHEME"] || ENV["PROJECT_NAME"],
        export_method: "app-store",
        clean: true,
        xcargs: "-allowProvisioningUpdates"
      )
    end

    # 5) выгрузка в TestFlight
    pilot(skip_waiting_for_build_processing: true)
  end

  # опционально: ручное создание/обновление сертификатов
  lane :generate_certs do
    setup_ci
    app_store_connect_api_key(
      key_id: ENV["APPSTORE_KEY_ID"],
      issuer_id: ENV["APPSTORE_ISSUER_ID"],
      key_content: ENV["APPSTORE_P8"],
      is_key_content_base64: false
    )
    run_match(readonly: false)
  end
end
